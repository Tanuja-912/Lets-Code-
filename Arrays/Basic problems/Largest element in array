Java implementation

class Solution {
    public static int largest(int[] arr) {
        // code here
        int m=0;
        for(int i=0;i<arr.length;i++){
            m=Math.max(m,arr[i]);
        }
        return m;
    }
}
