Day - 4:
Single Number:
Time Complexity: O(n)
(Bit Manipulation Approach)

Java Implementation:

class Solution(object):
    def singleNumber(self, nums):
        res=0
        for i in range(len(nums)):
            res=res^nums[i]
        return res
